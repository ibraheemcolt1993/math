(function () {
  if (window.katex) return;
  window.katex = {
    render: function (latex, element) {
      if (!element) return;
      element.textContent = latex || '';
      element.classList.add('katex');
    },
    renderToString: function (latex) {
      return latex || '';
    }
  };
})();
